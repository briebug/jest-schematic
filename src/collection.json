{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "ng-add": {
      "description": "Adds Jest to the application",
      "factory": "./jest/index",
      "schema": "./jest/schema.json"
    },
    "jest": {
      "description": "Adds Jest to the application",
      "factory": "./jest/index",
      "schema": "./jest/schema.json"
    },
    "add": {
      "description": "Adds Jest to the application",
      "factory": "./jest/index",
      "schema": "./jest/schema.json"
    }
  }
}
